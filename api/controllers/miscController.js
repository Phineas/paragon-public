const NodeCache = require("node-cache"),
      request = require("request-promise"),
      async = require("async");

/*exports.get_urban_definition = function(req, res) {
  const toDefine = req.params.term;

  request({
    uri: "http://api.urbandictionary.com/v0/define?term=" +
  });
};
*/
